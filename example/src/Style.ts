import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        color: 'black',
        borderRadius: 2,
        fontSize: 15,
        borderColor: 'black',
        paddingLeft: 10,
        padding: 0,
        borderWidth: 1,
        height:35
    }
})
